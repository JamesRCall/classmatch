# Contributing (Branching Policy)

- Default branches: `main`, `develop`
- Create feature branches from `develop`: `feature/<short-name>`
- Open PRs into `develop`; squash merge after review
- Periodically merge `develop` -> `main` for releases
